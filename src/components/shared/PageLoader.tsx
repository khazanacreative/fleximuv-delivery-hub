
import { Loader2 } from "lucide-react";

const PageLoader = () => (
  <div className="w-full h-60 flex items-center justify-center">
    <Loader2 className="w-8 h-8 animate-spin text-fleximov-500" />
  </div>
);

export default PageLoader;
